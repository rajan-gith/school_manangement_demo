<!DOCTYPE html>
<html>
  <head>
    <title>SchoolManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
          <span class="fs-4">SchoolManagement</span>
        </a>

        <ul class="nav nav-pills">
          <% if current_student %>
              <li class="nav-item active">
                <%= link_to 'Log Out', destroy_student_session_path, class: 'me-3 py-2 text-dark text-decoration-none', data: { "turbo-method": :delete } %>
              </li>
          <% elsif current_admin %>
              <li class="nav-item active">
                <%= link_to 'Log Out', destroy_admin_session_path, class: 'me-3 py-2 text-dark text-decoration-none', data: { "turbo-method": :delete } %>
              </li>
          <% elsif current_school_admin %>
              <li class="nav-item active">
                <%= link_to 'Log Out', destroy_school_admin_session_path, class: 'me-3 py-2 text-dark text-decoration-none', data: { "turbo-method": :delete } %>
              </li>
          <% else %>
            <li class="nav-item">
              <a href="<%= new_student_registration_path %>" class="me-3 py-2 text-dark text-decoration-none " aria-current="page">Student Sign up</a>
            </li>
            <li class="nav-item">
              <a href="<%= new_student_session_path %>" class="me-3 py-2 text-dark text-decoration-none">Log in</a>
            </li>
            <li class="nav-item">
              <a href="<%= new_admin_session_path %>" class="me-3 py-2 text-dark text-decoration-none">Admin Corner</a>
            </li>
            <li class="nav-item">
              <a href="<%= new_school_admin_session_path %>" class="me-3 py-2 text-dark text-decoration-none">School Admin Corner</a>
            </li>
          <% end %>
        </ul>
      </header>
    </div>
    <%= yield %>
  </body>
</html>

